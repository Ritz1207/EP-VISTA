# EP-VISTA V1.0 测试证据

记录日期：2026-09-01（中国标准时间，UTC+08:00）。本文件记录V1.0源码冻结前实际执行的检查；对应固定源码由Git标签`V1.0`标识，精确Commit SHA及源码归档SHA-256保存在仓库外的`release_artifacts/V1.0/`清单中。

## 验证环境

- 操作系统：Windows build 10.0.26200（Windows 11验证环境；Python平台字符串显示为Windows-10）
- Python：3.11.16
- NumPy：2.4.6
- pymsis：0.12.0
- Matplotlib：3.11.1
- PySide6：6.11.2
- pytest：9.1.1

## 实际结果

1. 完整自动化测试：`python -m pytest -p no:cacheprovider -q`，最终结果为`70 passed in 17.53s`。
2. 演示输入校验：`python run_ep_vista_cli.py validate examples/battery_mass_demo.ep-vista.json`，结果为“输入验证通过”。
3. 100小时演示计算：`python run_ep_vista_cli.py run examples/battery_mass_demo.ep-vista.json`，完成4,836个任务采样点及73个起始单圈采样点；比较3个候选方案，其中2个满足当前程序定义的每圈平均补偿、供电和设备范围判据。
4. 版本一致性：公开版本`V1.0`、Python包版本`1.0`与源码常量检查通过。
5. 公开卫生扫描：未发现163邮箱、本机用户目录、D盘绝对项目路径、常见私钥头、GitHub令牌、OpenAI样式密钥或AWS访问密钥模式。
6. 模型公式文档本地链接检查：共检查46个仓库内链接，目标文件均存在。

## 证据边界

- 自动化测试和演示运行属于功能回归与冒烟检查，不是物理模型验证、设备认证或采样收敛证明。
- 本次使用已有`ep-vista`环境，没有重新安装依赖，也没有完成全新机器或跨平台复现。
- Qt测试使用offscreen模式，没有替代人工GUI视觉检查。
- 环境中未安装gitleaks；密钥检查由项目内公开卫生测试和正则扫描完成，不能保证覆盖所有秘密格式。
- 随附天气、推进器来源和再分发边界仍以`docs/DATA_SOURCES.md`、`licenses/THIRD_PARTY_NOTICES.md`和发布检查清单为准。
- 本次未创建GitHub Release，`CITATION.md`与`CITATION.cff`没有填写尚不存在的仓库地址、发布日期或DOI。
