# 参与EP-VISTA

欢迎通过Issue讨论问题、提出改进建议，并通过PR贡献。项目已确定AGPL开源源码及 [NRLMSIS窄范围附加许可](ADDITIONAL_PERMISSION.md) 的发布策略；第三方模型和数据权利仍须单独核对。收到PR不等于已批准合并或已授予额外第三方使用权。

## 提交Issue

请提供程序版本、操作系统、Python和依赖版本、复现步骤、期望与实际结果。优先提供无隐私的最小合成项目，不要上传整个workspace、真实未公开任务、缓存、完整运行环境或未获准分发的数据。错误截图也请遮盖个人路径。数据问题须说明单位、日期范围和来源。

## 提交PR

1. 先用Issue说明问题及改动范围，特别是模型、物理假设、文件格式或默认数据变更。
2. 保持改动集中；新增数据必须说明来源、使用和再分发依据。不能把“网上可下载”当作许可证明。
3. 附带复现步骤、改动前后结果及针对性测试。涉及计算时，区分功能回归、数值对比和正式物理验证。
4. UI变更附截图；数据格式变更说明旧项目如何读取、ID冲突如何处理、单位怎样校验。
5. 不提交个人项目、运行缓存、模型二进制、密钥或本机绝对路径。运行前先确认模型使用权限。

## 开发环境与验证

请先阅读 [README 的运行要求与模型许可提示](README.md#安装与启动)。在仓库根目录创建并激活运行环境，再安装开发额外依赖：

```powershell
conda env create -f environment.yml
conda activate ep-vista
python -m pip install -e ".[dev]"
```

如果已完成 README 中的环境配置，只需激活该环境并执行最后一条命令。`dev` 依赖在 [pyproject.toml](pyproject.toml) 中声明，当前固定 pytest 9.1.1；pytest 不属于普通运行依赖。可编辑安装保留对本源码目录的引用，不能据此将安装后的模块脱离 `data/` 和 `examples/` 单独分发。

安装命令需要网络并会改变所选环境，建议使用专用 Conda 环境；使用已有完整环境进行验证时不必重复安装。

### 回归与示例检查

```powershell
python -m pytest -q
python run_ep_vista_cli.py validate examples/battery_mass_demo.ep-vista.json
```

pytest 的测试目录和 `src` 导入路径已在 `pyproject.toml` 配置，无须手动设置全局 `PYTHONPATH`。现有测试使用 Qt 的 offscreen 模式，不要求启动交互式桌面窗口；在无显示环境仍须具备 Qt 所需运行库。

测试覆盖型号库合并读写、异常拒绝、版本冲突、备份、项目快照、部分 GUI 行为及 CLI 短算例。测试会调用 pymsis/NRLMSIS 并可能生成本地计算缓存，必须在有权使用该模型的环境中运行。GUI 自动化不等于完整视觉验收。

`validate` 不执行大气计算，但当前导入结构仍加载 pymsis；输入校验通过也不代表天气已覆盖任务全程。空白模板的必填项有意留空，不应把它当作必须通过运行校验的完整示例。

完整演示与界面检查可在上述环境中执行：

```powershell
python run_ep_vista_cli.py run examples/battery_mass_demo.ep-vista.json
python run_ep_vista_gui.py
```

GUI 中检查新建、打开演示、另存、查看结果和关闭窗口；修改型号库前备份，避免把个人数据变更纳入提交。功能回归、输入校验和短算例不等于正式物理验证，涉及计算的改动还需数值对比和采样收敛检查。

发布前应在独立目录中检查完整源码树能否运行，不依赖父目录或本机路径；新环境安装、跨平台兼容和界面检查应分别记录，不能用已有环境测试代替。数据引用和来源要求见 [数据来源与引用](docs/DATA_SOURCES.md)。

## 贡献与权利

提交者须有权贡献相应内容。自研程序贡献拟按AGPL-3.0-or-later许可；是否同时授予 [NRLMSIS附加许可](ADDITIONAL_PERMISSION.md) 须由贡献者明确确认，不能因复制了现有源码头或提交PR就推定已获得所需权利。源码中的 `LicenseRef-EP-VISTA-AGPL-3.0-or-later-NRLMSIS` 仅用于确实同时适用这两项条款的代码。原创说明按NOTICE列出的文档许可。第三方材料不能被PR重新授权。贡献者保留自己的著作权，PR和普通签署声明不会自动转让著作权或授予维护者商业再许可权。

现有NOTICE保留未来单独商业许可的可能性。因此实质性外部贡献在合并前，维护者须分别记录AGPL授权、NRLMSIS附加许可和商业再许可的权利依据；没有相应授权时，不得把该贡献宣称为具有本例外或纳入不同条款的再许可。不同意附加许可的贡献须在合并前另行评估组合兼容性。这是上游合并审核要求，不剥夺下游依AGPL第7条移除附加许可的权利，也不是要求贡献者无条件转让权利或已经签署的协议。

欢迎合法修改和派生；请保留原作者及第三方署名，不得把他人成果冒充自己的。科研引用方式见CITATION.cff；不要求仅因使用程序就给原作者论文共同署名。
